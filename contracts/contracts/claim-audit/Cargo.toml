[package]
name = "claim-audit"
version = "0.1.0"
edition = "2021"
publish = false
description = "Append-only audit log invoked by vat-refund (inter-contract)"

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
